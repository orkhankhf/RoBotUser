<UserControl x:Class="RoBotUserApp.Pages.DataOperations"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="DataOperationsControl"
             Width="1400" Height="1000"
             Background="#FFFAFAFA">
    <Grid Margin="10">
        <!-- Define 2 columns -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <!-- Define 5 rows -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Example elements for each cell -->
        <!-- Row 1 -->
        <Button x:Name="LoadNewNumbersBtn" Content="Yeni nömrələri yüklə" Margin="0" Grid.Column="0" Grid.Row="0" Click="LoadNewNumbersBtn_Click" />
    </Grid>
</UserControl>
